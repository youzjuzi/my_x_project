server:
  port: 9999
spring:
  config:
    activate:
      on-profile: prod
  datasource:
    username: zyc
    password: YouZi20221850$$
    url: jdbc:mysql://youzi-mysql.mysql.database.azure.com:3306/xdb?useSSL=true&requireSSL=true&allowPublicKeyRetrieval=true
    driver-class-name: com.mysql.cj.jdbc.Driver

  data:
    redis:
      port: 6379
      host: localhost

logging:
  level:
    root: info
    com.diy: info
    org.springframework: warn
    org.mybatis: warn
    com.baomidou.mybatisplus: warn

mybatis-plus:
  global-config:
    db-config:
      logic-delete-field: deleted
      logic-not-delete-value: 0
      logic-delete-value: 1
  type-aliases-package: com.diy.*.entity