server:
  port: 9999
spring:
  config:
    activate:
      on-profile: prod
  datasource:
    username: root
    password: Aa1234
    url: jdbc:mysql://10.130.92.113/xdb?useSSL=false
    driver-class-name: com.mysql.cj.jdbc.Driver

  data:
    redis:
      port: 6379
      host: localhost

logging:
  level:
    com.diy: debug

mybatis-plus:
  global-config:
    db-config:
      logic-delete-field: deleted
      logic-not-delete-value: 0
      logic-delete-value: 1
  type-aliases-package: com.diy.*.entity